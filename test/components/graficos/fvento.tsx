import { expect } from 'chai';
import * as React from 'react';

// import ChartComponent from '../../../src/components/graficos/lineChart/fvento/index';

describe('TESTE UNITÁRIO - Inicialização do componente de gráfico fVento', () => {
    it('Deve iniciar o gráfico de fVento corretamente', () => {
        // expect(ChartComponent);
        // expect(ChartComponent.prototype instanceof React.Component);
        // expect(ChartComponent.prototype.render);
        // expect(ChartComponent instanceof React.Component);
    });
});